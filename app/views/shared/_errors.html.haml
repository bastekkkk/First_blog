- if obj.errors.any?
  .error_explanation
    .alert.alert-danger
      The from contains
      =pluralize(obj.errors.count, "error")
      %ul
      - obj.errors.full_messages.each do |msg|
        %li
        = msg
