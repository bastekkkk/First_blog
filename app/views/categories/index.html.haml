%h1{align: "center"} Listning all categories
= will_paginate
- @categories.each do |d|
  %ul.listing
    .row
      .well.col-md-4.offset-4
        %li.article_title
          =link_to "#{d.name}", category_path(d)
